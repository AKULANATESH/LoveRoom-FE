# Copy this file to .env and restart `npm run dev`
VITE_ENVIRONMENT_NAME=local
VITE_BACKEND_API_URL=http://localhost:3000

# Production build (Firebase Hosting) — set before `npm run build`:
# VITE_ENVIRONMENT_NAME=production
# VITE_BACKEND_API_URL=https://<your-render-service>.onrender.com
